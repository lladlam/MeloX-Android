# 更新日志

## 0.4.5 — 2026-08-26

### 新音乐源与账号

- 新增酷我音乐原生 Provider：支持搜索、播放链接解析、lyricx 歌词解码与手机号验证码登录。
- 酷我音乐走公共移动端接口，无需 Cookie；登录接口使用 MD5 签名鉴权，登录态本地持久化。

### 歌词与播放体验

- 统一自动歌词源顺序：不管什么音乐源，均按 AMLL → QQ音乐 → 网易云音乐 → 当前源 依次尝试。
- 长音高光效果增强：范围扩大 50%、亮度提高 35%、放大动画增强 25%。
- 修复 lyricx 字级时间戳未剥离导致歌词显示异常的问题。

### 播放器与界面

- 设置 → 外观新增「沉浸式播放」开关：全屏播放器打开后自动隐藏系统顶部状态栏，并保持封面与歌曲信息位置不变。
- 修复隐藏状态栏后播放器内容上跳的问题，使用稳定状态栏高度保留顶部内边距。

### 修复与稳定性

- 修复酷狗音乐串台问题。
- 修复更新检测在部分标签格式下判断错误的问题。
- 酷我接口统一使用 HTTPS，解决 Android 明文流量限制；允许 `ar.i.kuwo.cn` 明文登录域名。

### 构建与版本

- 版本号升级为 `0.4.5`，`versionCode 13`；正式产物名为 `MeloX-Android-0.4.5.apk`。
- Release APK 使用 MeloX 发布证书签名。

## 0.4.4-Beta — 2026-08-25

### 多音乐源、账号与更新链路

- 增加 Spotify OAuth 与账号入口，完善网易云、QQ音乐手机号登录以及多来源账号状态管理。
- 统一 GitHub DoH、GhFast、GhProxy 和 GhProxy.org 路由，自动并发测速并复用于静态更新清单、APK 下载和已授权云控配置。
- 不可用歌曲支持从已启用来源严格匹配完整音源；标题、主艺人和时长不满足条件时拒绝回退，不使用试听片段或绕过平台权限。
- 完善 QQ音乐、酷狗、Bilibili 等来源的播放身份、实际音质、歌词选择和缓存隔离。

### 云控、隐私与安全

- 增加用户主动授权、ECDSA P-256 签名验证、防回滚、版本范围和白名单约束的音乐源远程兼容配置；拒绝或关闭后不请求、不缓存、不应用。
- 云控仅可调整 APK 已内置的音乐源能力、回退顺序、超时、熔断和单条公告，不能下发代码、任意 API、广告、遥测或账号数据访问逻辑。
- 协议升级至 1.2；授权后每次应用进入前台检查配置，前台持续运行期间每两小时检查一次，后台不执行定时轮询。
- 增加签名配置状态、手动检查、缓存清理、验签公告及 `once`/`daily` 展示频率。

### 页面、搜索与深色模式

- 专辑详情复用歌单详情渲染器；搜索主结果和详情歌曲列表支持下一首播放、稍后播放、添加到资料库及长按操作。
- 迁移 Mei 风格的歌曲百科、个人中心、歌手和播客页面，统一固定标题、玻璃卡片、Hero、节目播放与横向专辑布局。
- 修复独立 Activity 未提供主题内容色导致的深色模式黑字，并补齐暗色 Surface Variant 令牌。
- 主页移除整体最大宽度和外层 gutter，改为全宽贯通视口，仅在各标题和横向列表内容上保留安全边距。

### 歌词、播放与无尽模式

- AMLL 保持第一阶段优先级，有效歌词不再被后续 QQ 评分覆盖；网易搜索恢复时长，失败后并发回退其他来源，并保留 45 秒总等待上限。
- 修复歌词等待段上一句瞬间变暗，恢复平滑渐暗与多行过渡。
- 无尽播放支持单曲空队列结束后恢复播放、失败冷却重试和第三方歌曲严格映射；进入队尾歌曲时立即预取，兜底窗口扩大至 60 秒并并行搜索种子。
- 版本号升级为 `0.4.4-Beta`，`versionCode 12`；正式产物名为 `MeloX-Android-0.4.4-Beta.apk`。

## 0.4.3-Dev — 2026-08-24

### AutoMix 与播放

- 增加持久化音频分析索引、后台批量分析和媒体预取，改进 AutoMix 候选曲目匹配与过渡置信度。
- 修复交叉淡化期间暂停、上一首、下一首和进度跳转造成的双播放器状态错乱，并保持 MediaSession 与界面时间线同步。
- 同步持久化播放音质，清理切歌后的陈旧实际音质状态；网易云播放缓存按歌曲、账号、请求音质和解析版本隔离。

### 歌词与界面

- 重构歌词时间线处理，支持任意数量的重叠活跃歌词，并保留逐行独立的弹性级联、焦点接位和离屏预布局。
- 修复逐字歌词切行突然压暗，长音增加低幅双轴动态并遵守系统“减少动态效果”设置。
- 移除共享玻璃组件外围边框，完善折叠标题、顶部渐隐模糊、安全存储清理和一起听页面。

### 歌单交互与构建

- 歌单歌曲行移除更多按钮，改为长按打开操作菜单；双向滑动提供下一首播放、稍后播放、收藏和移除操作。
- 增加可配置的右滑满滑操作、阻尼停靠、胶囊拉伸、触觉反馈，并修复收藏后“我喜欢的音乐”缓存未同步。
- 更新应用启动器图标；版本号升级为 `0.4.3-Dev`，`versionCode 11`。
- Release APK 使用 MeloX 发布证书签名，正式产物名为 `MeloX-Android-0.4.3-Dev.apk`。

## 0.4.2-Dev — 2026-08-23

### 音乐源、搜索与播放

- 增加 Bilibili 音乐源与账号入口，完善多来源播放资源解析和运行时 Provider 状态。
- 改进统一搜索聚合、同曲去重、来源可用性与音质选择，减少同名结果长期偏向单一来源。
- 修复 AutoMix 前台/后台预载、队尾自动补充、双播放器时间线误清理和开关状态同步。

### 私信与界面

- 将网易云私信迁移为 Mei 风格的 iOS Grouped List 会话列表与联系人选择页。
- 增加 Liquid Glass 顶部操作按钮、浮动输入栏、消息时间分组、歌曲/歌单/专辑资源卡片和长按复制。
- 私信继续位于“设置 → 扩展 → 私信与站内分享”，进入后隐藏底部 Dock。
- 搜索请求改为按下搜索键或停止输入 1.5 秒后触发，减少网易云 405 请求。
- 更新液态玻璃滑块、歌词、MiniPlayer 和多来源页面的交互与状态同步。

### 更新与构建

- 更新检查增加 GitHub Release / jsDelivr CDN 下载源选择；自动模式在 CDN 不可用时回退 GitHub Release。
- 版本号升级为 `0.4.2-Dev`，`versionCode 10`。
- Release APK 使用 MeloX 发布证书签名，正式产物名为 `MeloX-Android-0.4.2-Dev.apk`。

## 0.4.1-Beta — 2026-08-22

### 歌词与播放器

- 增加 AMLL TTML、QQ Music QRC、网易云 YRC 的逐字、对唱和伴唱歌词支持，并自动选择更合适的歌词来源。
- 修复逐字歌词在均衡/低质量及罗马音、多行场景下退化为整行匀速高亮的问题，默认使用高质量完整渲染。
- 优化 MiniPlayer 尺寸、队列切歌手势、实时音频波形和页面切换动画。

### 账号与音乐服务

- 增加网易云、QQ 音乐和酷狗的独立第二播放账号，只用于音频地址和可用音质探测。
- 网易云登录默认进入手机号登录页并保留二维码回退；QQ 音乐恢复稳定的网页登录流程。
- 修复第二账号登录页面层级、网易云主账号 Cookie 误识别和登录完成协程取消问题。

### 界面与稳定性

- 使用 SF Symbols 子集字体统一图标，并重构歌曲、歌单操作弹层及提示弹窗样式。
- 修复 Kotlin 文件类未进入增量 DEX 导致的启动崩溃，并提高 Gradle/D8 构建内存上限。
- 第二账号说明改为无标题提示，清除操作改为带确认的按钮；提示弹窗面板保持完全不透明。

### 构建

- 版本号升级为 `0.4.1-Beta`，`versionCode 9`。
- Release APK 使用既有 MeloX 发布证书签名，正式产物名为 `MeloX-Android-0.4.1-Beta.apk`。

## 0.4.0-Beta — 2026-08-20

### 业务闭环与多音乐源

- 增加公开/私密歌单创建、从自己的歌单移除歌曲，并把创建入口接入“添加到歌单”流程。
- 集合下载统一支持音质选择、多选、全选/取消；专辑、歌单和排行榜不再绕过批量下载确认。
- 下载保留 app-private 可靠副本，同时支持导出 MediaStore；本地音乐增加艺术家、专辑和文件夹分类以及缺失记录/孤立文件修复。
- 统一歌单、专辑、歌手、排行榜和播客节目详情；Provider 按 capability 暴露入口，不再显示不可用的自动来源回退占位设置。
- 恢复 31 Hz–16 kHz 十段均衡器和完整预设，补齐设置搜索、通知歌词模板、存储维护、更新日志、许可和诊断入口。

### 界面、组件与歌词

- Home 增加混合尺寸内容与三行歌曲区块；Explore 增加 hero，并按宽度使用 2/3/4 列。
- Home、Explore、Search、Library 和集合详情采用统一窗口策略，覆盖手机、折叠屏、平板和横屏。
- 补齐 Slider、Stepper、Bottom Search Toolbar、Context/Popup Menu、ActionSheet、Alert、Modal Surface 和 Color Picker 等高层玻璃组件。
- 增加 Apple Music / Classic 播放器外壳选择；Classic 支持手机、平板和沉浸横屏布局。
- 18 套生产 TextPV 风格具有独立视觉签名，并用单元测试防止模板退化合并。
- 增加英文、简体中文和繁体中文基础资源，补充关键图标语义和未知图标的安全回退。

### 性能、稳定性与构建

- 取消视觉背景的整曲重复解码；暂停、后台和非歌词页停止无效帧钟及高频轮询。
- 共享进程级 OkHttp/Provider Registry，增加有界 HTTP/Media3/动态取色/AutoMix 缓存和播放资源 single-flight。
- 将下载索引、Library 序列化与文件维护迁移到 IO，限制歌词重组和高成本玻璃/模糊路径。
- 启用 R8、资源收缩和 Material Symbols 子集化，纳入 Gradle wrapper、Baseline Profile 与 Macrobenchmark 旅程。
- 发布门禁通过 `testDebugUnitTest`、`lintDebug`、Debug/Release 构建和基准模块 assemble；真机 FPS、功耗、温升和启动耗时留给设备 Macrobenchmark/Perfetto，不使用推测数据。

### 构建

- 版本号升级为 `0.4.0-Beta`，`versionCode 8`。
- Release APK 使用既有 MeloX 发布证书签名，正式产物名为 `MeloX-Android-0.4.0-Beta.apk`。

## 0.3.4-Dev — 2026-08-17

### 歌词与播放器

- 修复逐字歌词切换到下一句时先整句闪白、再从首字重新高亮的问题。
- 长间奏期间使用正常的未播放歌词状态，并清除上一句残留焦点，结束后直接激活下一句。
- 等待三点组件与歌词共用滚动、缩放和透明度动画，避免进入视口后突然弹出。
- 同时间点的多行歌词以首行作为固定焦点锚点，其余行保持连续显示与滚动。
- 切歌时歌词回到固定起始位置；拖动进度不再重置 Apple Music 三层旋转背景。
- 完善跨语种翻译显示及时间匹配，避免部分歌词行缺少翻译。
- 扩展歌词页底部全宽模糊与触控拦截，修复空白区域点击穿透。

### 云盘、服务与界面

- 修复网易云音乐云盘分页、上传令牌、NOS 文件传输和歌曲信息提交兼容性。
- 云盘上传按音频格式发送正确 MIME，增加长文件超时、元数据清理及 EAPI/WEAPI 回退。
- 更多菜单和音质弹层随背景自适应颜色，并调整 Liquid Glass 位置和更多按钮图标。
- 精简音乐服务入口，设置页账号可直接进入音乐服务页面，并修复窄屏标题排版。

### 构建

- 版本号升级为 `0.3.4-Dev`，`versionCode 7`。
- Release APK 使用 MeloX 发布证书签名。

## 0.3.3-Dev — 2026-08-15

### iOS 风格界面与 Liquid Glass

- 对齐 MeloX / Mei 的首页、发现、音乐库、设置、搜索和服务页结构。
- 接入 AndroidLiquidGlass 公共 Demo 的按压高光、阻尼拖拽、折射、悬浮和速度形变效果。
- 修复 Dock 强调组件的拖拽、点击切换、边界裁切和动画卡顿问题。
- 修复标题与图标基线、Material Symbols 齿轮和箭头裁切问题。
- 将 MiniPlayer 改为真实采样背景的 Liquid Glass 表面。

### 音乐库与服务

- 增加独立音乐服务页，支持音乐源、账号和跨平台搜索配置。
- 收紧音乐库标题和分段控件布局，移除歌曲行重复灰色卡片遮罩。
- 云盘嵌入音乐库时使用紧凑操作栏，避免重复的大标题。

### 构建

- 版本号升级为 `0.3.3-Dev`，`versionCode 6`。

## 0.3.2-Dev — 2026-08-14

### 界面与 Liquid Glass

- 按 MeloX iOS 与 Apple 的分层思路重构播放器、底栏、弹层和通用操作组件。
- 引入基于 AndroidLiquidGlass Backdrop 的原生玻璃模块，统一玻璃色调、边缘、折射、阴影和降级样式。
- 调整首页、发现、搜索、音乐库、设置、歌曲操作和音质选择界面的布局与图标。
- 使用 MeloX iOS 官方应用图标作为 Android 启动器图标，并提供传统及 Adaptive Icon 资源。

### 播放器与歌词

- 重构全屏播放器、MiniPlayer、封面页、歌词页和播放队列之间的共享过渡。
- 统一低、中、高三档歌词的业务与动画逻辑；三档仅保留刷新率、模糊采样和背景质量等性能预算差异。
- 修复白色逐字歌词上浮时原位置残留灰色字形的问题。
- 修复视口底部歌词按整行渐隐后突然显现的问题；屏幕内内容由视口自然裁切。
- 对齐 MeloX iOS 的后续两行预加载，减少歌词进入底部边缘时的突显。
- 修复播放控制栏隐藏后，全屏透明触控层阻断歌词浏览的问题。
- 保留逐字高光、长音缩放、发光、回弹、焦点交接、翻译、罗马音和间奏倒计时。

### 性能与稳定性

- 将动态流光背景的低分辨率网格计算移出 UI 主线程，并按歌词画质控制刷新预算。
- 减少非活动歌词行的逐帧重组、整行重复绘制和不可见行布局观察。
- 优化 AutoMix 音频分析、均衡器状态同步、播放服务维护任务和完整曲目分析策略。
- 修复下载状态、播放命令与部分缓存更新中的重复工作和竞态问题。
- 在 Snapdragon 4 Gen 2 与 Dimensity 9500s 设备上完成低端与高端档位回归；9500s High 档稳定歌词测试未出现 FrameTimeline 卡顿或应用级崩溃。

### 兼容性

- 最低系统版本保持 Android 8.0（API 26）。
- HyperOS 焦点通知、超级岛、Lyricon、悬浮歌词和标准 Android MediaSession 路径保持可用。

> 这是开发版本。第三方音乐接口、OEM 私有能力和不同 GPU 合成器仍可能产生兼容性差异。
